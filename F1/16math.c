#include<stdio.h>
#include<math.h>

int main () {
    int  x = 9;
    float y = 3.59;

    //x = sqrt(x);
    //x = pow(x,2);
    //y = round(y);
    //y = floor(y);
    //y = ceil(y);
    //x = abs(x);
    //x = log(x);

    printf("%d",x);
}